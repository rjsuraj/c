main()
{
    int length,breadth;
    printf("enter l and b of rectangle :\n");
    scanf("%d",&length);
    scanf("%d",&breadth);
    printf("area of rectangle = %d",length*breadth);
    getch();
}